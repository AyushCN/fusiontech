# anchor.yaml — Your pipeline definition.
#   anchor generate   → produces .github/workflows/main.yml
#   anchor local      → runs the pipeline locally via Docker

name: "CI Pipeline"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    blueprint: "go-app"
    runs-on: ubuntu-latest
    steps:
      - run: "echo Hello World"
